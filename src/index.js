//first line will allow to use all rects components all of its library to build application
import React from "react";
//this line is a separate package that puts react on to the browser
import ReactDom from "react-dom";
import App from "./App";

ReactDom.render( < App / > , document.getElementById("root"));